import { ref } from 'vue'

export default {
  setup() {
    // logika komponenty
    // deklarujte zde nějaký reaktivní stav

    return {
      // toto je vystaveno do šablony (template)
    }
  }
}
